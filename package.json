{
  "name": "lesson5",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx http-server -p 3000 -o",
    "dev": "npx http-server -p 3000 -o",
    "build": "echo \"Build completed\"",
    "git:init": "git init && git add . && git commit -m \"初始化项目\"",
    "git:remote": "git remote add origin https://gitee.com/your-username/your-repo.git",
    "git:push": "git push -u origin master",
    "git:setup": "npm run git:init && npm run git:remote && npm run git:push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
